<view class="tab-control">
  <block wx:for="{{titles}}" wx:key="*this">
    <view class="control-inner {{currentIndex === index ? 'active' : ''}}" bind:tap="tabBarClick" data-index="{{index}}">
      <text class="contorl-text">{{item}}</text>
    </view>
  </block>
</view>